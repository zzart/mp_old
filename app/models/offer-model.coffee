Chaplin = require 'chaplin'

module.exports = class Offer extends Chaplin.Model
    #url: 'http://localhost:8000/api/schema/'
    #initialize: ->
    #    @.fetch()
    #    @schema = @.attributes







        # for i in list
        #     @default.push = {i: ''}
        # console.log(@default)
        #
        #class Schema1 extends Backbone.Model
        #    schema:
        #        textname:       {type:'Text', value:'', title:'fd'}
  # Mixin a synchronization state machine
  # _(@prototype).extend Chaplin.SyncMachine
    #models
            #select (lots options)  title:      { type: 'Select', options: ['Mr', 'Mrs', 'Ms'] }
            #multiselect            multiselect: {type:'Checkboxes', template:'radio', options: [], data_role:'controlgroup'}
            #bool                   published:  {type: 'Select', options: ['on', 'off'], data_role: 'slider'}
            #text                   name:       'Text'
            #number                 tel:        {type: 'Number'}
            #text (password)        password:   'Password'
            #textarea               description:'TextArea'
            #radio                  address: {type:'Radio', template:'radio', options: ['1','2'], data_role:'controlgroup'}
